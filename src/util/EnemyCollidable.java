package util;

/** New interface providing methods for entities that can collide with Enemy  */
public interface EnemyCollidable {
    /** Method used for handling what happens when colliding with an Enemy object */
    public void checkEnemy();
}
